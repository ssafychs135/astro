# 🚀 C:\PJT 프로젝트 통합 분석 보고서 (기술 심층 분석판)

본 문서는 `C:\PJT` 디렉토리에 위치한 3개의 프로젝트에 대한 기술적 정체성, 핵심 로직 및 비즈니스 가치 창출 방식을 심층 분석한 결과입니다.

---

## 1. 🔵 Blue Fox (S14P11A702)
**정체성: "Byte Buddy 기반의 무중단 분산 추적 시스템"**

### 🧠 핵심 기술 구현
- **Java Agent 트레이싱**: `premain` 단계에서 `AgentBuilder`를 통해 `DispatcherServlet.doDispatch`, `java.sql.Connection.prepareStatement` 등을 가로챕니다.
- **바이트코드 조작**: 애플리케이션 코드를 수정하지 않고 런타임에 성능 측정 로직(`Advice`)을 주입하여 HTTP 요청부터 SQL 실행까지의 전체 생명주기를 추적합니다.
- **데이터 파이프라인**: 수집된 `Span` 데이터를 외부 `TraceSender`를 통해 비동기로 전송하여 타겟 애플리케이션의 성능 저하를 최소화합니다.

### 🎯 비즈니스 가치
- **운영 안정성**: 코드 변경 없이 에이전트 부착만으로 성능 가시성을 확보합니다.
- **정밀한 병목 진단**: 메서드 단위의 실행 시간과 실제 실행된 SQL 쿼리를 매칭하여 AI가 코드 수준의 최적화 가이드를 제공하게 합니다.

---

## 2. 🍷 Jupasu (S14P21A505)
**정체성: "pgvector와 하이브리드 검색 기반의 와인 인텔리전스"**

### 🧠 핵심 기술 구현
- **벡터 유사도 검색**: `pgvector`의 `<=>` (Cosine Distance) 연산자를 사용하여 와인 임베딩 간의 유사도를 계산, 최적의 와인을 추천합니다.
- **하이브리드 검색**: 단순 키워드 매칭(`word_similarity`)과 고차원 벡터 검색을 결합하여 검색어의 오타나 모호한 표현에도 정확한 결과를 도출합니다.
- **모듈형 AI 서버**: `FastAPI`를 통해 OCR(라벨 인식), LLM(상담), RAG(정보 추출)를 독립된 서비스로 운영하며 백엔드와 `WebClient`로 통신합니다.

### 🎯 비즈니스 가치
- **직관적 사용자 경험**: 사진 촬영 한 번으로 복잡한 와인 정보를 즉시 파악하고, 개인의 취향(임베딩)에 기반한 고도화된 추천을 제공합니다.

---

## 3. 🔍 Team9 Project (team9)
**정체성: "Node.js 워커와 RAG 체인을 결합한 동영상/리뷰 분석 플랫폼"**

### 🧠 핵심 기술 구현
- **분산 비동기 워커**: `Node.js` 서버가 유튜브 자막 수집(`fetchCaptions`) 및 Gemini API 연동과 같은 비동기 I/O를 전담하여 처리 성능을 높였습니다.
- **Self-Querying RAG**: `LangChain`의 `SelfQueryRetriever`를 사용하여 자연어 질문을 "특정 모델명", "특정 날짜 범위" 등의 구조화된 필터로 변환하여 검색 신뢰도를 확보합니다.
- **지능형 라우팅**: 질문의 의도를 분석하여 RAG(정보 검색)와 일반 대화 체인을 분리 실행함으로써 비용과 응답 속도를 최적화합니다.

### 🎯 비즈니스 가치
- **정보 요약 및 의사결정 지원**: 긴 동영상 콘텐츠를 AI 요약으로 빠르게 소비하고, 방대한 리뷰 데이터 중 나에게 꼭 필요한 정보만 선별하여 제공받을 수 있습니다.

---
*최종 업데이트: 2026년 4월 7일*
