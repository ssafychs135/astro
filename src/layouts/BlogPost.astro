---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate, heroImage } = Astro.props;
---

<html lang="ko">
	<head>
		<BaseHead title={title} description={description} />
	</head>

	<body class="bg-bg-base text-text-base antialiased">
		<Header />
		<main class="max-w-5xl mx-auto px-6 py-24" transition:animate="fade">
			<article>
				<header class="mb-16">
					<div class="tech-label mb-8">
						<span class="text-primary">> Research_Insights // Meta_Data</span>
						<span class="opacity-30">Classification: Strategic_Analysis</span>
					</div>
					<h1 class="text-5xl md:text-8xl font-black tracking-tighter uppercase mb-12 leading-[0.9]">
						{title}
					</h1>
					
					<p class="text-xl md:text-2xl text-text-muted leading-relaxed max-w-3xl mb-12">
						{description}
					</p>

					<div class="telemetry-bar">
						<div class="telemetry-item">
							<span class="tech-label opacity-40 !text-[9px]">Timestamp</span>
							<span class="font-bold"><FormattedDate date={pubDate} /></span>
						</div>
						<div class="telemetry-item">
							<span class="tech-label opacity-40 !text-[9px]">Document_Status</span>
							<span class="font-bold uppercase tech-font text-xs tracking-widest text-primary">Verified_Insight</span>
						</div>
						{updatedDate && (
							<div class="telemetry-item">
								<span class="tech-label opacity-40 !text-[9px]">Last_Update</span>
								<span class="font-bold"><FormattedDate date={updatedDate} /></span>
							</div>
						)}
					</div>
				</header>

				{heroImage && (
					<div class="blueprint-frame">
						<div class="rounded-sm overflow-hidden border border-white/5">
							<Image width={1200} height={600} src={heroImage} alt="" class="w-full h-auto grayscale hover:grayscale-0 transition-all duration-1000" />
						</div>
					</div>
				)}
				
				<div class="prose-obsidian max-w-none">
					<slot />
				</div>
			</article>
		</main>
		<Footer />
	</body>
</html>
